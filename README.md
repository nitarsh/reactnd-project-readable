
# How to start and run
Navigate to the project server folder root.
Type and enter `npm install` and `export SET PORT=5001; npm start` to start the project server.
Navigate to the react front-end project folder root.
Type and enter`npm install` and `npm start` to start the react server
Open `localhost:3000` in your browser 